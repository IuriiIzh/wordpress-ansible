- name: Read service variables from vars.yml 
  hosts: "*, localhost"
  tasks:
    - name: Include vars vars.yaml
      ansible.builtin.include_vars:
        file: vars.yml

- name: Install and configure WordPress
  hosts: servers
  become: yes
  roles:
    - wordpress
